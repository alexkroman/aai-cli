mode: optimize  # optimize | eval

eval:
  max_samples: 50
  prompt: "Transcribe verbatim."
  num_threads: 12

datasets:
  earnings22:
    path: sanchit-gandhi/earnings22_robust_split
    config: default
    audio_column: audio
    text_column: sentence
    split: test
  peoples:
    path: fixie-ai/peoples_speech
    config: clean
    audio_column: audio
    text_column: text
    split: test
  ami:
    path: edinburghcstr/ami
    config: ihm
    audio_column: audio
    text_column: text
    split: test

optimization:
  samples: 100
  iterations: 10
  starting_prompt: >-
    Transcribe verbatim.
  candidates_per_step: 8
  trajectory_size: 3
  seed: 42
  num_threads: 12
  llm_model: claude-opus-4-6
  meta_every: 3  # run meta-optimization every N iterations (0 = disabled)

hydra:
  output_subdir: null
  run:
    dir: .
